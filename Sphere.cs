using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Sphere : MonoBehaviour
{
    public Node node;
    public int asdasd;
    // Start is called before the first frame update
    void Start()
    {
        asdasd=node.employee.satisfaction;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
